# Site settings
title: 鱼虾写字
email: readonlyfile@hotmail.com
description: > # this means to ignore newlines until "baseurl:"
  不要害怕，专心抓鱼
baseurl: "" # the subpath of your site, e.g. /blog/
url: "https://fseasy.github.io" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

plugins: 
  - jekyll-paginate
  - jekyll-sitemap

exclude: [_site, md_templates]
paginate: 10

#social info
github_username:  fseasy

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      permalink: /:year/:month/:title/
